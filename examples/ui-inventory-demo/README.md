# UI Asset Inventory demo

Three reachable pages share one accessible “保存” button style through the common visual tokens. The historical fixture path remains explicit so `scripts/ui_inventory_acceptance.py` can verify that the read-only inventory reports no cross-page style drift or near-color candidates; synthetic variants in the aggregation unit test retain detector coverage. This is not a production consistency claim.
